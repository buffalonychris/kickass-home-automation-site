<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Smart Home Packages | KickAss Home Automation</title>
  <link rel="preload" href="/assets/styles.css" as="style">
  <link rel="stylesheet" href="/assets/styles.css">
  <style>
    .page-hero { padding:3rem 1rem 1rem; text-align:center; }
    .page-hero h1 { font-size:clamp(1.8rem,3vw,2.6rem); margin:0 0 .5rem; }
    .sub { color:#6b7280; max-width:900px; margin:0 auto; }
    .grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(300px,1fr)); gap:1rem; margin:2rem 0 3rem; }
    .plan { border:1px solid #e5e7eb; border-radius:1rem; background:#fff; padding:1.25rem; display:flex; flex-direction:column; }
    .plan h2 { margin:.2rem 0 .25rem; }
    .pill { display:inline-block; font-size:.8rem; background:#111827; color:#fff; padding:.2rem .6rem; border-radius:999px; }
    .features { margin:.5rem 0 1rem; }
    .features li { margin:.25rem 0; }
    .cta-row { margin-top:auto; display:flex; gap:.5rem; flex-wrap:wrap; }
    .btn { padding:.6rem 1rem; border:1px solid #111827; border-radius:.6rem; text-decoration:none; }
    .btn.primary { background:#111827; color:#fff; }
    .note { font-size:.9rem; color:#6b7280; margin-top:.25rem; }
    .compare { border:1px dashed #e5e7eb; border-radius:.75rem; padding:1rem; }
  </style>
  <script defer src="/assets/nav-loader.js"></script>
</head>
<body>
  <header id="site-nav"></header>

  <main class="container">
    <section class="page-hero">
      <h1>Curated Packages</h1>
<button class='open-quote cta-button'>Get a Quote</button>
      <p class="sub">Three clear starting points. All local‑first Z‑Wave with room to grow. We’ll tailor counts and locations during the walkthrough.</p>
    </section>

    <section class="grid">
      <article class="plan">
        <span class="pill">Starter</span>
        <h2>Smart Basics</h2>
        <p>Hands‑free lighting, door access, and essential leak protection.</p>
        <ul class="features">
          <li>Z‑Box hub (local control, scene builder)</li>
          <li>4× Z‑Wave switches/dimmers (hall, bath, kitchen, bedroom)</li>
          <li>2× Motion sensors (hall + bath)</li>
          <li>2× Leak sensors (sink + washer)</li>
          <li>Titan valve actuator add‑ready</li>
        </ul>
        <div class="compare">
          <strong>Great for:</strong> condos, townhomes, first smart home.
        </div>
        <div class="cta-row">
          <a class="btn primary" href="/contact.html">Request Quote</a>
          <a class="btn" href="/automation-ideas-starter.html">See Starter Ideas</a>
        </div>
      </article>

      <article class="plan">
        <span class="pill">Comfort</span>
        <h2>Home Comfort &amp; Safety</h2>
        <p>Add humidity‑driven fans, thermostat scenes, and full leak auto‑shutoff.</p>
        <ul class="features">
          <li>Everything in Starter</li>
          <li>Titan valve actuator + 2 additional leak sensors</li>
          <li>1× Thermostat integration</li>
          <li>2× Additional dimmers (living + primary)</li>
          <li>1× Scene controller (Welcome/Night/Away)</li>
        </ul>
        <div class="compare">
          <strong>Great for:</strong> single‑family homes, busy households.
        </div>
        <div class="cta-row">
          <a class="btn primary" href="/contact.html">Request Quote</a>
          <a class="btn" href="/automation-ideas.html">Browse Ideas</a>
        </div>
      </article>

      <article class="plan">
        <span class="pill">Estate</span>
        <h2>Whole‑Property Control</h2>
        <p>Long‑range coverage to gates, garages, shops, and guest spaces.</p>
        <ul class="features">
          <li>Everything in Comfort</li>
          <li>ZWLR (Long Range) stick + LR devices</li>
          <li>Gate/garage relays + tilt sensors</li>
          <li>Outdoor lighting + deck scene pad</li>
          <li>Guest access codes + arrival scenes</li>
        </ul>
        <div class="compare">
          <strong>Great for:</strong> large lots, multiple buildings, acreage.
        </div>
        <div class="cta-row">
          <a class="btn primary" href="/contact.html">Request Quote</a>
          <a class="btn" href="/automation-ideas-luxury.html">See Estate Ideas</a>
        </div>
        <p class="note">We’ll map distances and recommend LR vs mesh during the site survey.</p>
      </article>
    </section>

    <section class="compare" style="margin-bottom:3rem;">
      <h3>What’s next?</h3>
      <p>Pick a package to start, then cherry‑pick from our <a href="/automation-ideas.html">Ideas &amp; Combos</a> library. We’ll right‑size hardware and finalize scenes after a walkthrough.</p>
    </section>
  </main>

  <footer id="site-footer"></footer>

<!-- Get a Quote Modal -->
<div id="quoteModal" class="modal">
  <div class="modal-content">
    <span class="close">&times;</span>
    <h2>Request a Quote</h2>
    <form id="quoteForm">
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" required>
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required>
      <label for="details">Project Details:</label>
      <textarea id="details" name="details" rows="4" required></textarea>
      <button type="submit" class="cta-button">Send Request</button>
    </form>
  </div>
</div>

<script>
document.addEventListener("DOMContentLoaded", function() {
  var modal = document.getElementById("quoteModal");
  var span = document.getElementsByClassName("close")[0];

  document.querySelectorAll(".open-quote").forEach(btn => {
    btn.onclick = function() {
      modal.style.display = "block";
    };
  });

  span.onclick = function() {
    modal.style.display = "none";
  };

  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  };
});
</script>

<style>
.modal { display:none; position:fixed; z-index:1000; padding-top:60px;
  left:0; top:0; width:100%; height:100%; overflow:auto; background-color:rgba(0,0,0,0.4); }
.modal-content { background-color:#fefefe; margin:auto; padding:20px; border:1px solid #888;
  width:80%; max-width:500px; border-radius:8px; }
.close { color:#aaa; float:right; font-size:28px; font-weight:bold; cursor:pointer; }
.close:hover { color:black; }
</style>


<!-- Quote Modal -->
<div id="quote-modal" class="modal hidden" role="dialog" aria-modal="true" aria-labelledby="quote-title">
  <div class="modal-backdrop" data-close="true"></div>
  <div class="modal-content">
    <button class="modal-close" data-close="true" aria-label="Close">×</button>
    <h2 id="quote-title">Get a Quote</h2>
    <p>Tell us what you want to automate. We’ll follow up with a tailored plan and pricing.</p>
    <form id="quote-form">
      <label>
        Name
        <input type="text" name="name" required />
      </label>
      <label>
        Email
        <input type="email" name="email" required />
      </label>
      <label>
        Phone (optional)
        <input type="tel" name="phone" />
      </label>
      <label>
        What do you want to automate?
        <textarea name="notes" rows="4" placeholder="e.g., Flood protection + gate + hot tub lighting" required></textarea>
      </label>
      <button type="submit" class="btn primary">Send</button>
      <p class="form-note">No spam—just smart ideas.</p>
    </form>
  </div>
</div>
<style>
  /* minimal modal styles; uses your existing CSS variables if present */
  .modal.hidden { display: none; }
  .modal { position: fixed; inset: 0; z-index: 1000; }
  .modal-backdrop { position: absolute; inset:0; background: rgba(0,0,0,.5); }
  .modal-content { position: relative; margin: 6vh auto; max-width: 560px; background: #fff; color: #111; border-radius: 12px; padding: 24px; box-shadow: 0 10px 40px rgba(0,0,0,.2); }
  .modal-close { position: absolute; top: 8px; right: 12px; border: 0; background: transparent; font-size: 24px; cursor: pointer; }
  #quote-form { display: grid; gap: 12px; }
  #quote-form input, #quote-form textarea { width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 8px; }
  .btn.primary { background: var(--accent, #0ea5e9); color: #fff; border: 0; padding: 10px 16px; border-radius: 10px; cursor: pointer; }
  .form-note { font-size: .9rem; opacity: .75; margin-top: 6px;}
</style>
<script>
(function(){
  const modal = document.getElementById('quote-modal');
  function openModal(prefill){
    if (prefill) {
      const notes = modal.querySelector('textarea[name="notes"]');
      if (notes && !notes.value) notes.value = prefill;
    }
    modal.classList.remove('hidden');
    document.body.style.overflow = 'hidden';
  }
  function closeModal(){
    modal.classList.add('hidden');
    document.body.style.overflow = '';
  }
  document.addEventListener('click', function(e){
    const btn = e.target.closest('.cta-quote');
    if (btn) {
      const prefill = btn.getAttribute('data-prefill') || '';
      openModal(prefill);
    }
    if (e.target.matches('[data-close]')) {
      closeModal();
    }
  });
  document.getElementById('quote-form')?.addEventListener('submit', function(e){
    e.preventDefault();
    // In production, POST to your backend or a form service endpoint here.
    alert('Thanks! We'll be in touch shortly.');
    e.target.reset();
    closeModal();
  });
})();
</script>

</body>
</html>